\setglossarypreamble{
\addcontentsline{toc}{chapter}{Symbols and Mathematical Prerequisites}
Throughout this thesis, we follow a consistent notation and try to use conventional notations and notations introduced in the used literature. Here we will provide a quick overview over the notation.

$\mathbb{R}$ and $\mathbb{Q}$ denote the real and rational numbers respectively, with restrictions indicated via subscripts (e.g., $\mathbb{R}_{>0}$ and $\mathbb{R}_{\geq 0}$).
Other sets are generally denoted with uppercase letters, except for the letters $F$ and $G$, which are real-valued functions from the AD model and are used throughout this thesis.\\
Dimensions are represented by $n$ and $i, k$ and $m$ are integers used for indexing or counting.\\
Vectors are bold (e.g., $\ve{x}, \ve{y}$) with coordinates $\ve{x} = (x_1, \dots, x_n)$. Sequences of vectors are indexed as $\ve{x_i}$ or $\ve{x^{(i)}}$.\\
Greek letters are either real-valued functions and have a special meaning (such as $\varphi, \delta$ or $\gamma$) or constants defined in the context (e.g., $\alpha, \beta$).
Other symbols are used according to conventions, e.g., $\lambda$ for convex combinations, $\varepsilon$ for small real quantities and $|x|$ for the absolute value of $x$.


Any symbols not mentioned here will be introduced as needed within the relevant sections.
}




% Metric spaces and topology
\glsxtrnewsymbol[description={
A metric space
}, 
group={ms-topo}]{metricSpace}
{\ensuremath{\langle X, \metric \rangle}}


\glsxtrnewsymbol[description={
  The open $(r)$ or closed $[r]$ ball with radius \( r \) around \( 0 \) w.r.t. the metric \( \metric \)
}, 
group={ms-topo}]{openball}
{\ensuremath{\ball{r} / \cball{r}}}

\glsxtrnewsymbol[description={
A metric function. Depending on the context, this refers to an arbitrary metric or the metric from the segmentally additive AD model
}, 
group={ms-topo}]{metric}
{\ensuremath{\metric}}

\glsxtrnewsymbol[description={
A Minkowski (or power) metric
}, 
group={ms-topo}]{minkMetric}
{\ensuremath{\metric_{r}}}

\glsxtrnewsymbol[description={
The maximum metric (Tchebychev distance)
}, 
group={ms-topo}]{max-metr}
{\ensuremath{\metric_{\infty}}}

\glsxtrnewsymbol[description={
A parametric curve
}, 
group={ms-topo}]{Pcurve}
{\ensuremath{\curve}}

\glsxtrnewsymbol[description={
The length of a parametric curve
}, 
group={ms-topo}]{Pcurve-len}
{\ensuremath{\len}}

\glsxtrnewsymbol[description={
The convex hull of \( A \)
}, 
group={ms-topo}]{convhull}
{\ensuremath{\co(A)}}

\glsxtrnewsymbol[description={
The extreme points of \( A \)
}, 
group={ms-topo}]{extr-point}
{\ensuremath{\extr(A)}}


\glsxtrnewsymbol[description={
  A proximity structure, most of the time a factorial PS. Elements denoted by $\ps{a}, \ps{b}, \ps{c}$ and so on.
}, 
group={ps}]{PS}
{\ensuremath{\langle \ps{A}, \succsim \rangle}}


\glsxtrnewsymbol[description={
The ternary relation on a proximity structure
}, 
group={ps}]{ps-tern-rel}
{\ensuremath{\langle \ps{a}\ps{b}\ps{c} \rangle}}

\glsxtrnewsymbol[description={
\( \ps{a, b} \) and \( \ps{c} \) differ only on one factor, and \( \ps{b} \) is between \( \ps{a} \) and \( \ps{c} \)
}, 
group={ps}]{ps-abc-between}
{\ensuremath{\ps{a}|\ps{b}|\ps{c}}}

% ---- functions ----
\glsxtrnewsymbol[description={
A real-valued function defined on a proximity structure
}, 
group={func}]{ADPhi}
{\ensuremath{\varphi_i}}

\glsxtrnewsymbol[description={
The coordinate function in the AD Model
}, 
group={func}]{ADFi}
{\ensuremath{F_i}}




% ---- other symbols ----
\glsxtrnewsymbol[description={
The quasi-arithmetic \( \phi \)-mean of \( \ve{x} \) with weights \( \ve{w} \)
}, group={other}]
{quasi-arith-mean}{\ensuremath{\mean{\ve{x}}}}

\glsxtrnewsymbol[description={
For $p > 0$ the $p$-th power mean, for $p = 0$, the geometric mean of $\ve{x}$ with weights $\ve{w}$
}, group={other}]{powermean}
{\ensuremath{\meanp{\ve{x}}}}



\renewcommand\glstreegroupheaderfmt[1]{\begingroup\centering \textbf{#1}\par\endgroup}
\glsfindwidesttoplevelname

\glsxtrsetgrouptitle{ps}{Proximity Structures}
\glsxtrsetgrouptitle{func}{Functions}
\glsxtrsetgrouptitle{ms-topo}{Metric Spaces and Topology}
\glsxtrsetgrouptitle{other}{Other symbols}
